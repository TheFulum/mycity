<?xml version="1.0" encoding="utf-8"?>
<!--
    BottomSheet для верификации по SMS.
    Фикс: поля ввода и кнопка теперь не перекрываются —
    всё внутри вертикального LinearLayout без ConstraintLayout.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_bottom_sheet"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="12dp"
    android:paddingBottom="32dp">

    <!-- Drag handle -->
    <View
        android:layout_width="36dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="24dp"
        android:background="@drawable/bg_drag_handle"/>

    <!-- Заголовок -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Подтверждение номера"
        android:textColor="#F1F5F9"
        android:textSize="20sp"
        android:textStyle="bold"/>

    <!-- Подсказка: на какой номер ушёл код -->
    <TextView
        android:id="@+id/tvPhoneHint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="#7C8FA8"
        android:textSize="14sp"
        android:lineSpacingMultiplier="1.3"/>

    <View
        android:layout_width="40dp"
        android:layout_height="2dp"
        android:layout_marginTop="16dp"
        android:background="#3D5AFE"/>

    <!-- Блок ввода кода (скрыт до получения верификационного ID) -->
    <LinearLayout
        android:id="@+id/layoutOtp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="24dp"
        android:visibility="gone">

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/tilOtp"
            style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Код из SMS"
            app:boxStrokeColor="#3D5AFE"
            app:boxStrokeWidth="1.5dp"
            app:hintTextColor="#7C8FA8"
            app:boxBackgroundColor="#0D1E3A"
            app:counterEnabled="true"
            app:counterMaxLength="6">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etOtp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="numberPassword"
                android:maxLength="6"
                android:textColor="#F1F5F9"
                android:textSize="22sp"
                android:letterSpacing="0.3"
                android:gravity="center"/>
        </com.google.android.material.textfield.TextInputLayout>

        <!-- ProgressBar — показывается во время отправки/верификации -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center"
            android:layout_marginTop="16dp"
            android:visibility="gone"
            android:indeterminateTint="#3D5AFE"/>

        <!-- Кнопка верификации — гарантированно ниже поля -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnVerify"
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:layout_marginTop="16dp"
            android:text="ПОДТВЕРДИТЬ"
            android:textSize="14sp"
            android:letterSpacing="0.06"
            android:textColor="#FFFFFF"
            android:backgroundTint="#3D5AFE"
            app:cornerRadius="16dp"/>

        <!-- Отправить повторно -->
        <TextView
            android:id="@+id/tvResend"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="16dp"
            android:text="Отправить код повторно"
            android:textColor="#3D5AFE"
            android:textSize="14sp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:visibility="gone"/>

    </LinearLayout>

    <!-- Состояние ожидания (пока SMS ещё не пришла) -->
    <LinearLayout
        android:id="@+id/layoutWaiting"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="28dp">

        <ProgressBar
            android:id="@+id/progressBarSend"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:indeterminateTint="#3D5AFE"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="Отправляем SMS..."
            android:textColor="#7C8FA8"
            android:textSize="14sp"/>

    </LinearLayout>

</LinearLayout>